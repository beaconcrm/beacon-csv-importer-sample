/*
* A method to map a `row` object into a Beacon-friendly format, to be
* inserted into the database.
*
* Here is where you need to write your mapping code!
*
* Tip: we recommend splitting out your "mappers" into separate files, with
* separate unit test files for each.
*/

const mapRow = (row: Record<string, string>): Record<string, unknown> => {
  const mappedRow: Record<string, unknown> = {};

  // DO YOUR MAPPING HERE TO ADD TO `mappedRow`!

  return mappedRow;
};

export default mapRow;
